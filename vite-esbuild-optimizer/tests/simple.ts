import assert from 'assert'

it('ready', () => {
    assert(true)
})

// async function main() {
//   await esbuild({
//     entryPoints: ['lodash-es'],
//     bundle: true,
//     splitting: true,
//     format: 'esm',
//     outdir: 'dist'
//   })
// }

// main()
