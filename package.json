{
    "private": true,
    
    "workspaces": [
        "vite-esbuild-optimizer",
        "example",
        "example-linked-package",
        "esbuild-polyfills"
    ],
    "scripts": {
        "watch": "tsc -b -w",
        "test": "jest"
    },
    "devDependencies": {
        "jest": "^26.6.1"
    }
}
